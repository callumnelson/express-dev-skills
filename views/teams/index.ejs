<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  <% teams.forEach( team => { %>
    <li>
      <form
        action="/todos/<%= team._id %>?_method=DELETE"
        class="delete-form"
        method="POST"
      >
        <button type="submit">X</button>
      </form>
      <a href="/teams/<%= team._id %>"><%= team.teamName %> (<%= team.abbreviation%>)</a>
    </li>
  <% }) %>
  <br>
  <a href="/teams/new">Add a team</a>
  <br>
  <a href="/">Home page</a>
</body>
</html>